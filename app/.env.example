# Optional. Local development only, never set this on a public deployment: Vite
# inlines VITE_* variables into the client bundle, so anyone could read it.
# Raises GitHub's rate limit from 60 to 5,000 requests per hour. Create a token
# with NO scopes (public data only), then copy this file to .env.local.
VITE_GITHUB_TOKEN=
